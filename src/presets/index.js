import { preset as legacy }         from './legacy'
import { preset as rect }           from './rect'
import { preset as shades_classic } from './shades-classic'
import { preset as shades_grey }    from './shades-grey'

export const Presets = {
  legacy: legacy,
  shades_classic: shades_classic,
  shades_grey: shades_grey,
  rect: rect
}