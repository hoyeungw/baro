import { layout as legacy }         from './legacy'
import { layout as rect }           from './rect'
import { layout as shades_classic } from './shades-classic'
import { layout as shades_grey }    from './shades-grey'

export const Layouts = {
  legacy: legacy,
  shades_classic: shades_classic,
  shades_grey: shades_grey,
  rect: rect
}